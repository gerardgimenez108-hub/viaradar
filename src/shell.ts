import type {MessageKey} from "./i18n.ts";
export function shell(t:(key:MessageKey)=>string):string { return `<header class="topbar"><a class="brand" href="/" aria-label="${t("home")}"><span class="brandmark" aria-hidden="true">V<span>R</span></span>ViaRadar<span class="beta">BETA</span></a><div class="header-actions"><label class="language-control"><span>${t("language")}</span><select id="language" aria-label="${t("language")}"><option value="auto">${t("automatic")}</option><option value="es">Español</option><option value="en">English</option></select></label><button id="install" class="quiet">${t("install")} <span aria-hidden="true">↗</span></button></div></header><main><section class="station"><div><p class="eyebrow">RODALIES · BARCELONA</p><h1>L’Hospitalet<span>de Llobregat</span></h1><p class="station-note">${t("tagline")}</p></div><div class="station-symbol" aria-hidden="true"><span>↗</span><small>72305</small></div></section><div id="connection" class="connection" role="status">${t("connecting")}</div><section class="board" aria-labelledby="board-title"><div class="board-heading"><div><p class="eyebrow">${t("departures")}</p><h2 id="board-title">${t("nextHours")}</h2></div><button id="refresh" class="refresh" aria-label="${t("refreshAria")}">↻ <span>${t("refresh")}</span></button></div><div class="filters"><label for="line">${t("line")}</label><select id="line"><option value="">${t("allLines")}</option></select><span class="timezone">${t("timezone")}</span></div><div class="table-head" aria-hidden="true"><span>${t("departure")}</span><span>${t("destination")}</span><span>${t("platform")}</span></div><div id="departures" aria-live="polite"><div class="empty"><span class="empty-icon">↗</span><h3>${t("loadingTitle")}</h3><p>${t("loadingBody")}</p></div></div><div class="board-footer"><span id="updated">${t("waiting")}</span><span>${t("autoRefresh")}</span></div></section><section class="guide" aria-labelledby="guide-title"><div><p class="eyebrow">${t("signals")}</p><h2 id="guide-title">${t("guideFirst")} <br>${t("guideSecond")}</h2></div><div class="guide-items"><p><strong><i class="dot official"></i> ${t("publishedRenfe")}</strong><span>${t("officialBody")}</span></p><p><strong><i class="dot prediction"></i> ${t("historicalEstimate")}</strong><span>${t("predictionBody")}</span></p><p><strong><i class="dot unknown"></i> ${t("notPublished")}</strong><span>${t("unknownBody")}</span></p></div></section><details class="details"><summary>${t("sourcesTitle")}</summary><div id="sources">${t("waitingSources")}</div><p>${t("publicData")} <a href="https://data.renfe.com/" target="_blank" rel="noreferrer">Renfe Data</a> · <a href="https://creativecommons.org/licenses/by/4.0/" target="_blank" rel="noreferrer">CC BY 4.0</a>. ${t("disclaimer")}</p></details><footer>ViaRadar <span>${t("footer")}</span></footer></main><dialog id="install-dialog"><form method="dialog"><button class="close" aria-label="${t("closeInstall")}">×</button><p class="eyebrow">${t("takeBoard")}</p><h2>${t("installTitle")}</h2><p><strong>iPhone / iPad:</strong> ${t("ios")}</p><p><strong>Android:</strong> ${t("android")}</p><p>${t("installNote")}</p><button class="refresh">${t("gotIt")}</button></form></dialog>`; }
